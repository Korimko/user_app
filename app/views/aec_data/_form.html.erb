<%= form_for(@aec_datum) do |f| %>
  <% if @aec_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aec_datum.errors.count, "error") %> prohibited this aec_datum from being saved:</h2>

      <ul>
      <% @aec_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
